# achiko

Install package:
```
npm install
```

To run application:
```
  npm run dev
```

Available Endpoints: 
- POST /login
=> to get access_token
- GET /profile
=> get profile based on the current logged in user (access_token needed)